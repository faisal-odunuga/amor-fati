Got it — I’ll upgrade the whole system properly and include **auto profile creation on signup** (clean, production-safe way with Supabase triggers).

This will be your **final blueprint + execution plan**.

---

# 🧠 🔥 COMPLETE SYSTEM SPEC (FINAL VERSION)

This is a **premium bookclub SaaS feature** with:

* Subdomain routing (`bookclub.` / `admin.`)
* AI-generated reading plans
* Progress tracking
* Testimonial automation
* Light gamification
* **Auto user profile creation (NEW)**

---

# 🗄️ 🧱 DATABASE (OPTIMIZED SCHEMA)

## 👤 `profiles` (AUTO-CREATED)

```sql
create table profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  email text unique,
  full_name text,
  avatar_url text,
  role text default 'member',
  created_at timestamp default now()
);
```

---

## ⚡ AUTO CREATE PROFILE (CRITICAL)

### ✅ Supabase Trigger (DO THIS)

```sql
create function public.handle_new_user()
returns trigger as $$
begin
  insert into public.profiles (id, email)
  values (new.id, new.email);
  return new;
end;
$$ language plpgsql security definer;
```

```sql
create trigger on_auth_user_created
after insert on auth.users
for each row execute procedure public.handle_new_user();
```

---

## 📚 `books`

```sql
create table books (
  id uuid primary key default gen_random_uuid(),
  title text not null,
  author text,
  cover_url text,
  created_at timestamp default now()
);
```

---

## 🗓️ `reading_plans`

```sql
create table reading_plans (
  id uuid primary key default gen_random_uuid(),
  book_id uuid references books(id),
  title text,
  start_date date,
  end_date date,
  created_by uuid references profiles(id),
  created_at timestamp default now()
);
```

---

## 📆 `schedule_items`

```sql
create table schedule_items (
  id uuid primary key default gen_random_uuid(),
  plan_id uuid references reading_plans(id) on delete cascade,
  date date,
  type text, -- reading | reflection | rest
  label text,
  weight int default 1,
  created_at timestamp default now()
);
```

---

## 📝 `daily_logs`

```sql
create table daily_logs (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references profiles(id),
  schedule_item_id uuid references schedule_items(id),
  nestuge_url text,
  key_insight text,
  action_taken text,
  created_at timestamp default now(),
  unique(user_id, schedule_item_id)
);
```

---

## 📊 `user_progress`

```sql
create table user_progress (
  user_id uuid references profiles(id),
  plan_id uuid references reading_plans(id),
  progress numeric default 0,
  completed_days int default 0,
  primary key (user_id, plan_id)
);
```

---

## 🔥 `streaks`

```sql
create table streaks (
  user_id uuid primary key references profiles(id),
  current_streak int default 0,
  longest_streak int default 0,
  last_log_date date
);
```

---

## 💬 `testimonials`

```sql
create table testimonials (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references profiles(id),
  content text,
  approved boolean default false,
  created_at timestamp default now()
);
```

---

# 🔐 RLS (ESSENTIAL)

## Profiles

```sql
create policy "Users can view own profile"
on profiles for select using (auth.uid() = id);
```

---

## Daily Logs

```sql
create policy "Users manage their logs"
on daily_logs
for all
using (auth.uid() = user_id);
```

---

## Admin Access

```sql
create policy "Admins full access"
on reading_plans
for all
using (
  exists (
    select 1 from profiles
    where id = auth.uid() and role = 'admin'
  )
);
```

---

# 🌐 🧭 SUBDOMAIN ARCHITECTURE

## Middleware (Next.js)

```ts
export function middleware(req) {
  const host = req.headers.get('host') || ''

  if (host.startsWith('admin.')) {
    return NextResponse.rewrite(new URL('/admin', req.url))
  }

  if (host.startsWith('bookclub.')) {
    return NextResponse.rewrite(new URL('/bookclub', req.url))
  }

  return NextResponse.rewrite(new URL('/', req.url))
}
```

---

# 🤖 AI PLAN GENERATION

## Prompt (FINAL VERSION)

```
You are generating a structured reading schedule.

INPUT:
- Chapters list
- Start date
- End date

RULES:
- Spread chapters evenly
- Add reflection days after every 3–4 chapters
- Add occasional rest days
- Return JSON only

OUTPUT:
[
  {
    "date": "YYYY-MM-DD",
    "type": "reading | reflection | rest",
    "label": "Chapter 1",
    "weight": 1
  }
]
```

---

# ⚙️ CORE FEATURES

---

## 1. 📊 Progress Engine

```ts
progress = completed_weight / total_weight
```

---

## 2. 🔥 Streak Logic

```ts
if (today === lastLogDate + 1) streak++
else if (today > lastLogDate + 1) streak = 1
```

---

## 3. 💬 Testimonial Automation

Trigger when:

* progress ≥ 70%

```ts
const content = `
I stayed consistent for ${completedDays} days.
One major shift: ${keyInsight}
I applied it by: ${actionTaken}
`
```

---

## 4. 🎮 Gamification (Clean, Premium)

* Streak indicator (🔥 5 days)
* Milestones:

  * 25%
  * 50%
  * 100%
* Completion screen:

  * “You executed.”

---

# 🛠️ 🧩 IMPLEMENTATION GUIDE (VERY IMPORTANT)

---

# 🚀 EXECUTION PLAN (STEP BY STEP)

---

## 🟢 STEP 1: Setup Project

* Next.js (App Router)
* Supabase project
* Env keys

---

## 🟢 STEP 2: Auth + Auto Profile

* Enable email auth
* Add trigger (VERY IMPORTANT)
* Test signup → profile auto created

---

## 🟢 STEP 3: Middleware Routing

* Setup subdomains:

  * `bookclub.yoursite.com`
  * `admin.yoursite.com`

---

## 🟢 STEP 4: Database Setup

* Create ALL tables
* Add RLS
* Seed:

  * 1 book
  * 1 admin user

---

## 🟢 STEP 5: Admin Panel (CORE)

Build:

### ➤ Create Plan Flow

1. Select book
2. Enter chapters
3. Select dates
4. Generate with AI
5. Preview (editable)
6. Save

---

## 🟢 STEP 6: Member Experience

### Dashboard

* Today’s task
* Progress %
* Streak

---

### Schedule Page

* Clean timeline

---

### Daily Log Modal

* Nestuge link
* Insight
* Action

---

## 🟢 STEP 7: Logic Layer

* Progress calculation
* Streak tracking
* Prevent duplicate logs

---

## 🟢 STEP 8: Advanced Features

### Testimonials

* Auto generate
* Store as pending

---

### Analytics (Admin)

* Completion rates
* Active users

---

### Resources

* PDFs via Supabase storage

---

# 🎯 FINAL RESULT

If you execute this properly:

👉 You’ll have:

* A real SaaS product
* A monetizable system
* A retention-focused experience

---

